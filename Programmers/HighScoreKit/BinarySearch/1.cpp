#include <string>
#include <vector>
#include <algorithm>

using namespace std;

long long solution(int n, vector<int> times) {
    long long answer = 28;
    
    sort(times.begin(), times.end());
    
    
    
    return answer;
}